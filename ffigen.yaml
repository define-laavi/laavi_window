# Run with `dart run ffigen --config ffigen.yaml`.
name: WindowElevenBindings
description: |
  Bindings for `src/window_eleven.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/window_eleven_bindings_generated.dart'
headers:
  entry-points:
    - 'src/window_eleven.h'
  include-directives:
    - 'src/window_eleven.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
